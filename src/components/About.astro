---
import type { Lang } from '../i18n/types';
import { getDict } from '../i18n/utils';
import Section, { type SectionTone } from './Section.astro';

interface Props {
  lang: Lang;
  tone?: SectionTone;
}

const { lang, tone } = Astro.props;
const dict = getDict(lang);
---

{/*
  The opening paragraph is set larger than the rest. Every paragraph on the page
  was the same size, so nothing told the eye where to start reading. Its measure
  is narrower to compensate for the bigger size.
*/}
<Section id="about" label={dict.about.label} tone={tone}>
  <div class="space-y-3">
    {
      dict.about.paragraphs.map((paragraph, index) =>
        index === 0 ? (
          <p class="measure text-[1.5rem] leading-[1.5]">{paragraph}</p>
        ) : (
          <p class="measure">{paragraph}</p>
        ),
      )
    }
  </div>
</Section>
