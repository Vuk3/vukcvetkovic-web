---
import { Mail } from '@lucide/astro';
import type { Lang } from '../i18n/types';
import { getDict } from '../i18n/utils';
import { site } from '../site';
import BrandIcon from './BrandIcon.astro';
import Section, { type SectionTone } from './Section.astro';

interface Props {
  lang: Lang;
  tone?: SectionTone;
}

const { lang, tone } = Astro.props;
const dict = getDict(lang);

/** Profile URLs read better without the scheme. */
const bare = (url: string) => url.replace(/^https?:\/\//, '').replace(/\/$/, '');

const entries = [
  {
    label: dict.contact.emailLabel,
    href: `mailto:${site.email}`,
    text: site.email,
    external: false,
    brand: null,
  },
  {
    label: dict.contact.linkedinLabel,
    href: site.links.linkedin,
    text: bare(site.links.linkedin),
    external: true,
    brand: 'linkedin' as const,
  },
  {
    label: dict.contact.githubLabel,
    href: site.links.github,
    text: bare(site.links.github),
    external: true,
    brand: 'github' as const,
  },
];
---

<Section id="contact" label={dict.contact.label} tone={tone}>
  <p class="measure text-muted">{dict.contact.intro}</p>

  <dl class="mt-5 space-y-2.5">
    {
      entries.map((entry) => (
        <div class="sm:flex sm:gap-5">
          <dt class="display flex items-center gap-2 text-[15px] text-muted sm:w-24 sm:shrink-0">
            {entry.brand ? (
              <BrandIcon name={entry.brand} class="size-3.5 shrink-0" />
            ) : (
              <Mail class="size-3.5 shrink-0" aria-hidden="true" />
            )}
            {entry.label}
          </dt>
          <dd>
            <a
              href={entry.href}
              target={entry.external ? '_blank' : undefined}
              rel={entry.external ? 'me noopener' : undefined}
              class="link"
            >
              {entry.text}
            </a>
          </dd>
        </div>
      ))
    }
  </dl>
</Section>
